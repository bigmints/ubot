# Final independent collection implementation review

Date: 2026-09-14. Reviewer: `/root/engine_plan_reviewer`, a separately delegated non-authoring Codex specialist. This reviewer has authored none of the implementation, package fixes, host fixes, benchmark or submitted implementation-validation report.

**Decision: seven native-story passes; S01 and S02 remain changes requested.** This is an exact-artifact, exact-work-revision implementation review, not aggregate product acceptance or release permission. It supersedes earlier defects only to the extent independently retested below. The [earlier independent changes-requested review](implementation-independent-signoff.md) remains unchanged as history, including the original defective artifact identity. Planning-only reviews also remain historical.

## Frozen identity

- Validation report: `docs/collections/implementation-validation.json`, SHA-256 `80d415cd54fb8c6545b48f7b3fa831f96abd4bc85877ed05924c2e027a9cc895`.
- Evidence directory: `docs/collections/evidence/implementation-validation-20260914T144328Z`.
- Exact tarball: `docs/collections/evidence/implementation-validation-20260914T144328Z/tested-youbot-collection-engine-0.1.0.tgz`, SHA-256 `0479c8be9f0262011f2be5d889905613dbfbd8fb745859a7820301dbbda1d676`, 66529 bytes. It was independently extracted outside the repository and used for the package/probe executions below; no later repacking substituted another artifact.
- Baseline HEAD: `d7e082ee88898a9abf4211a85115cc082c4b1df7`. The working tree is dirty; this HEAD alone is not the implementation identity. All **312** manifest file hashes matched before review and again after the independent executions; all recorded command stdout/stderr hashes matched their retained files.
- Source manifest: `docs/collections/evidence/implementation-validation-20260914T144328Z/source-manifest.json`. This new signoff is subsequent reviewer evidence and intentionally is not part of that earlier source manifest.
- Fresh reviewer actor: `codex:01a09f15-dbcd-7e02-8d2b-e13ed1738ea9:frozen-final-review-d9121ca4-593f-4d81-aa17-9400e22417b0`; session `session_cd3942e9-f6ba-4d91-91cc-7aad76e1b623`. Identity is locally caller-declared; actual independence is this separate non-authoring host specialist, not the actor label alone.
- Every native criterion was reread with `spacecrew work.get`; exact current revisions/statuses matched the table below and native acceptance text was unchanged.

## Independently executed checks

| Check | Exact command / basis | Observed result |
| --- | --- | --- |
| Original package edge reproductions | `node /tmp/collections-final-independent/review-reproductions.mjs`, importing only public exports from the extracted final tarball | PASS: duplicate-ID withdrawal, invalid calendar/reference rejection, incompatible currency handling, strong/candidate identity reconciliation, both manual-provenance forms, metadata and actual malformed-output rejection. Off-page expiry now returns STALE_EVIDENCE. |
| Mixed-unit filtering | `node /tmp/collections-final-independent/units-probe.mjs` | PASS: same numeric field across m2/ft2 collections without a unit yields INPUT_INCOMPLETE at arguments.filters[0].unit; it no longer returns both as an exact comparison. |
| Final package author tests on exact tarball | Copied current test fixture to extracted package test directory; `node --test /tmp/collections-final-independent/package/test/engine.test.mjs` |16/16 pass, including independent repository instances/cooperating processes, publication/undo, malformed dates/references, mixed units/currencies, pagination/expiry, reconciliation and schemas. These are independently executed author tests, distinct from the reviewer's probes. |
| Original host overlap reproduction | `node /Users/pretheesh/Projects/youbot/youbot-core/node_modules/vitest/vitest.mjs run --root /tmp/s08-independent-race race.test.ts` |1/1 pass. The exact earlier non-authoring test is unchanged (SHA below): two retry UUIDs while generation is held result in one model call. |
| Durable host authoring routes | `npm test -- src/concierge/collections/__tests__/routes.test.ts` from youbot-core |11/11 pass: text/PDF sources, restart, conflict payload retention, retry identity, overlapping claim, abandoned-runtime recovery and stale-result takeover fencing. |
| Broader host regression selection | `npm test -- src/concierge/collections/__tests__ src/engine/visitor-dispatch.test.ts src/engine/visitor-reply.test.ts src/concierge/__tests__/visitor-safety.test.ts src/concierge/__tests__/store.test.ts src/automation/approvals/visitor-routing.test.ts src/automation/followups/visitor-reply.test.ts` from youbot-core |10files/74tests pass. This review's selection/count is distinct from the submitted report's broader12-file runs; no counts are substituted. |
| Real compiled-handler pending dispatch | `node docs/collections/evidence/implementation-validation-20260914T144328Z/dispatch-window-regression.mjs` |PASS: withdrawal between first check and actual callback send returns stale evidence; sent=[] and handled=false. |
| Real compiled-handler remembered context | `node docs/collections/evidence/implementation-validation-20260914T144328Z/stale-session-regression.mjs` |PASS: after one published retrieval, withdrawal followed by a no-tool reply revalidates remembered evidence and sends nothing. |
| Focused UI static check | `./node_modules/.bin/eslint app/concierge/collections/page.tsx lib/concierge.ts` from web-ui |Exit0, no output. Source review also confirms saved job list/get hydration and organizing-state retry guards. No fresh browser/keyboard run is claimed. |

The generation/transport dependencies in host tests are controlled doubles. Actual engine/public tarball, filesystem persistence, PDF parser, route handlers and compiled dispatch code execute at their relevant test boundaries. The PDF benchmark was independently inspected and its recorded identity/command evidence checked; its24 parser runs were not rerun or relabeled as reviewer executions.

## Defect resolutions observed

- Search evidence now binds collection-qualified item identities and searched collection generations. Unpublishing either of two items with the same local ID invalidates the earlier combined receipt.
- Query evidence accounts for full-scope time boundaries: a paginated result with totalCount2 becomes stale when an off-page matching item expires and the current count becomes1.
- Exact currency ordering and unit filtering require compatible scope. Ambiguous currency/unit queries fail; explicit compatible ordering reports excluded rows rather than implying completeness.
- Calendar dates and scoped relation targets are validated. The former accepted2026-99-99/string-reference payloads now fail.
- Source-derived replacements preserve manual values even if the supplied evidence has a source ID but no field ID. The retained test now yields an explicit manual-correction issue and apply PROPOSAL_INVALID. Explicit source-backed replacement requires proper field provenance and review.
-19 tool definitions expose access/effect/audience metadata and concrete nested/result schemas. A deliberately corrupted actual repository result is rejected by dispatcher output validation.
- Durable authoring claims prevent overlapping local-host generation. Saved generated operations/issues/source references survive conflict and restart; abandoned-runtime takeover fences the delayed first completion from canonical proposal creation or job overwrite.

## Native story verdicts

| Story | Exact work revision | Verdict | Criterion-level basis and remaining action |
| --- | --- | --- | --- |
| CC-S01 | `work_62ce2939-c249-4b6d-b017-bdf73845c30d` / **12** | **changes_requested** | Criteria1/3 are supported: current docs identify JSON-file persistence and its transaction limits, live host authority, snapshot/query/dispatch contracts and existing provider configuration boundary. Criterion2 remains incomplete: the retained 24-run benchmark uses synthetic domain-shaped sparse PDFs, explicitly not a broad representative/dense customer corpus. It verifies250-page acceptance/251-page rejection but does not establish a file-processing runtime bound; parser has no application deadline. The harness60-second safety timeout is not an implemented parser limit. Record and validate the supported corpus/runtime decision before claiming this native criterion complete. |
| CC-S02 | `work_8127313b-be59-4886-a9db-ae19bba4d263` / **11** | **changes_requested** | Typed values, scoped existing-item references, history/separate published snapshots, checked concurrent writes, restart, invalid temporal rejection and current direct-query storage are supported by source, package16tests and independent probes. Native criterion1 also requires migrations. The JSON adapter currently accepts persistence schemaVersion1 and rejects unsupported formats; no supported upgrade/conversion or rollback execution proves preservation of prior collection data. Schema rejection and new-store/restart tests are not migration evidence. Keep this criterion pending until a versioned migration/compatibility fixture and persisted readback establish it. |
| CC-S03 | `work_a5b4a04d-d8b8-4908-a84a-e5fef0ad3a66` / **11** | **pass** | All three native supported-text/PDF intake criteria are supported: host-private original/versioned source records; configured byte/page/content bounds and honest coverage/terminal outcomes; real pdf-parse page/text preservation; idempotent source/proposal reuse after model unavailability and service/disk reload. Actual route11tests passed again, including source inspection, unsupported/partial/error handling and durable PDF retry. The package correctly starts at already-extracted input. This pass does not close S01 runtime/corpus limits or system/release gates. |
| CC-S04 | `work_15fc4f11-d23e-4678-a833-0be7253e5bcd` / **11** | **pass** | Native draft/provenance/unknown and reviewed-proposal boundaries are supported in the implemented generic/domain-schema scope. Independent public-tarball probes and package tests verify strong identifier matching to stable items, candidate ambiguity, explicit unresolved issues blocking apply, and preservation of manual fields for both field-linked and bare-source evidence. Invalid dates, invalid/cross-entity references and incompatible comparisons are rejected; source content invokes no engine file/model/network runner. Missing source items are not inferred deleted. Broad real-model property/class extraction accuracy remains a separate S11/S14 corpus limit. |
| CC-S05 | `work_034859e6-dadc-40cd-bf5f-cdc80b41e279` / **11** | **pass** | Bounded draft changes, revision checks, history/undo, exact reviewed publication, unpublish/archive and valid-through checks are supported. Unresolved proposals are blocked; private fields are filtered; draft edits retain prior published content. Independent duplicate-ID withdrawal and off-page expiry probes now reject stale evidence, while package tests cover archive/undo without silent republication and stale revision/intent rejection. Manual source-origin conflicts remain reviewable and cannot be applied silently. Full release/operational delivery is not inferred. |
| CC-S08 | `work_1e76d4bc-6f0d-4467-b866-1c4e7b80d062` / **11** | **pass** | Durable text/PDF job list/get/retry and UI reload hydration repair the prior gaps. Generated operations, issues and source IDs are persisted before proposal CAS; conflict/restart retry preserves them and avoids another model call. The unchanged independent overlap test now passes1/1, and canonical routes11/11 cover repeated retry identity,1source/1ingest/1proposal, atomic claim, abandoned-runtime recovery and fenced stale completion. Source inspection confirms in-progress retries cannot claim a second generation and UI disables retry for organizing state. Existing content-only conversation/inbox boundaries remain intact. Multi-process host deployment and long-running-provider deadlines are not established by these local-host tests. |
| CC-S09 | `work_8ca41fd4-d575-43f9-9d65-88fdc48a3a08` / **11** | **pass** | Independent exact-artifact tests now reject ambiguous mixed-currency ordering and mixed-unit filtering; explicit compatible scope reports exclusions honestly. Invalid calendar dates and relation targets fail validation. Duplicate local IDs retain collection-qualified evidence; both off-page publication changes and off-page expiry invalidate old query/count evidence. Direct canonical scans avoid delayed-index trust, and results identify exact counts/pagination/current supporting records. Package16tests and retained probes support the native typed/current-query contract. Broad domain corpus coverage remains a system-level limit. |
| CC-S10 | `work_87af54c0-fee9-4f86-9ac1-b05ee1ba85b7` / **11** | **pass** | Bounded read-only collection tools, visitor/owner-preview filtering and current supporting evidence are supported by host source and fresh backend10-file/74-test execution. The two preserved real compiled-handler regressions were independently rerun: withdrawal between first validation and send is held with zero sends, and a subsequent no-tool turn revalidates remembered withdrawn evidence with zero sends. Repaired multi-collection and time-bound evidence now supports these host checks. Existing answerFromKnowledge/takeover/approval/financial/booking protections remain covered by focused regressions; preview has no outbound delivery/mutation. No real provider/channel-wide delivery certification is claimed. |
| CC-S13 | `work_9b26ee09-86c7-4a40-9a45-5070439195fb` / **10** | **pass** | All native package/tool criteria are supported under approved revision3, which explicitly moves file/blob/parser/job/extraction responsibilities to the host. The19 exported tools now carry access/effect/audience metadata, bounded nested input contracts and operation-specific success/error output schemas; independent corrupted-state output probe is rejected by runtime output validation. Trusted context, visibility, idempotency/revision/intent and repaired domain/freshness checks execute beneath model arguments. Public ESM/CJS exports and zero runtime dependencies are verified in the retained external packed consumer. Source/model/file execution remains outside core; host durable jobs are separately verified. Two protocol fixtures establish normalization conformance, not live compatibility with two providers. |

## Unreviewed and system/release limits

S06, S07, S11, S12 and S14 are deliberately not reviewed or closed here. The final native passes above do not close their dependencies or grant aggregate readiness. Required design/accessibility evidence, broad properties/classes/session/timezone/recurrence and extraction-quality corpus, full39-scenario/32-criterion aggregation, installed clean-machine migration/restore and supported-platform release evidence remain separate. S01 and S02 explicitly stay unresolved rather than borrowing evidence from unrelated green tests.

The actual parser benchmark's generated sparse property/class/generic PDFs demonstrate local250/251-page enforcement, timing and process-memory observations only. Its60-second child harness timeout is not an application parser deadline, and approximately700MiB observed process peaks are not a production capacity guarantee. The JSON repository's10-second lock wait and operator stale-lock recovery also do not prove power-loss safety, upgrade conversion or multi-host reliability. No real-provider accuracy/cost, live channel-wide compatibility, external message delivery, package registry publication, deployment or release is established.

Factory remains the canonical product/document authority. Review pass does not alter approved revision3, grant product/design approval, or rewrite immutable native criteria. Original S13 file/blob/job/extraction wording is read with the explicit user-approved revision3 boundary: these concerns belong to host applications, and they are not reintroduced into the reusable engine. Native Codex MCP activation and Fikr Studio handoff remain separate recorded limitations; no external handoff is claimed here.

## Reproduction identities

| Reviewer reproduction | SHA-256 |
| --- | --- |
| `/tmp/collections-final-independent/review-reproductions.mjs` | `7f40f51cdb99c74ea9531dba442ec6b9421701a40cbd34c9ca5629a12f3aca4f` |
| `/tmp/collections-final-independent/units-probe.mjs` | `5bd43e48c56668f046bc81331b1cb8384438395d3ac398617568991a638ba9e3` |
| `/tmp/s08-independent-race/race.test.ts` | `bc2628ccd4f346c84c9663c7d3873cdeedc990c9855f56673e29e74c1097c2d6` |

To reproduce the three final edge checks through public tools: (1) publish two matching items, query pageSize1, advance the injected clock past only the off-page item's validThrough, then validate the old receipt; (2) propose a replacement of a manual field with sourceRevisionIds and source evidence lacking fieldId, then apply; (3) search numeric area fields in m2 and ft2 collections with the same raw gte filter and no unit. Expected final outcomes are STALE_EVIDENCE, an explicit unresolved manual conflict followed by PROPOSAL_INVALID, and INPUT_INCOMPLETE respectively. The frozen artifact produced all three.
